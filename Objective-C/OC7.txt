@class作用：简单的引用一个类（以前是用import引入类）
@class xxx ;
仅仅告诉编译器 xxx 是一个类，并不会包含类中的所有内容（即不会拷贝头文件）

.h文件中使用@class引用一个类
.m中使用#import包含这个类的.h文件
虽然略麻烦，但提升了编译器效率

第二个作用：两个类互相引入类

注意点：使用 @class 编译器仅仅是知道 xxx是一个类
并不能使编译器知道这个类有什么属性和方法
这就是 .m文件 需要 #import .h 的原因

禁止在A类 importB 类，同时B类 import A类